{
  "name": "wordpot-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cargo contract build --manifest-path ink/Cargo.toml",
    "test": "cargo contract test --manifest-path ink/Cargo.toml",
    "upload": "cargo contract upload --manifest-path ink/Cargo.toml",
    "instantiate": "cargo contract instantiate --manifest-path ink/Cargo.toml"
  },
  "devDependencies": {
    "dotenv": "^16.4.7"
  }
}
